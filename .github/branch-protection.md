# Main Branch Protection Baseline

The machine-readable source of truth is [`branch-protection-policy.json`](./branch-protection-policy.json).

Apply that policy to `main` and keep the remote GitHub protection settings aligned with it.
